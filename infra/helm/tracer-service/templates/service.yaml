apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-tracer-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: tracer-service

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-tracer-service
  labels:
    app: tracer-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: tracer-service
  template:
    metadata:
      labels:
        app: tracer-service
    spec:
      containers:
        - name: tracer-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          ports:
            - containerPort: 8080

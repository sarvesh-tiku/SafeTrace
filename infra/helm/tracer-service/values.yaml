replicaCount: 1

image:
  repository: your-ecr-repo/tracer-service
  tag: latest
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: "250m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

service:
  type: ClusterIP
  port: 8080
